<% if false %>
<%# This is the original AsciiBinder code %> 
<%- Dir.glob("_stylesheets/*").sort.each do |sheet| -%>
  <link href="<%= File.join(css_path, File.basename(sheet)) %>" rel="stylesheet" />
<%- end -%>
<% end %>

<%# Instead: Manually link to CSS stylesheets, in the order you want them pulled in. %>
<link href="https://fonts.googleapis.com/css?family=Roboto:100,100i,300,300i,400,400i,500,500i,700,700i,900,900i" rel="stylesheet">
<link href="<%= File.join(css_path, "bootstrap.min.css") %>" rel="stylesheet" />
<link href="<%= File.join(css_path, "bootstrap-theme.min.css") %>" rel="stylesheet" />
<link href="<%= File.join(css_path, "bootstrap-dialog.min.css") %>" rel="stylesheet" />
<link href="<%= File.join(css_path, "asciibinder.css") %>" rel="stylesheet" />
<link href="<%= File.join(css_path, "tw-search-docsite.css") %>" rel="stylesheet" />
<link href="<%= File.join(css_path, "tw-style-docsite.css") %>" rel="stylesheet" />
