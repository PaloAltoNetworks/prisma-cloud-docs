Retrieves a list of all rules that make up your compliance policy for containers.

```bash
$ curl -k \
  -u <USER> \
  -H 'Content-Type: application/json' \
  -X GET \
  https://<CONSOLE>:8083/api/v1/policies/compliance/container
```
