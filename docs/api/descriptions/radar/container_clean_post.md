Cleans the container runtime profiles and Radar entities.

```bash
$ curl -k \
  -u <USER> \
  -H 'Content-Type: application/json' \
  -X POST \
  https://<CONSOLE>:8083/api/v1/radar/container/clean
```
