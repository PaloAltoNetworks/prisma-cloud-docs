Returns the namespaces from the container view on the Radar page.

```bash
$ curl -k \
  -u <USER> \
  -H 'Content-Type: application/json' \
  -X GET \
  https://<CONSOLE>:8083/api/v1/radar/container/filters
```
